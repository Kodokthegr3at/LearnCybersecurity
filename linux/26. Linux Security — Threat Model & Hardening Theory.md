# 🛡️ Linux Security — Threat Model, Access Control & Hardening Theory

> **LearnCybersecurity** | Linux Fundamentals Series  
> 📅 Last Updated: 2026 | 👤 Author: kodoktheGr3at  
> 📚 Primary refs: Brian Ward — *How Linux Works*; William Stallings — *Network Security Essentials*

---


<!-- LC-CURRICULUM-START -->
> **Curriculum ID:** `LC-033` | **Phase 1:** Linux  
> **Est. study:** 4h | **Level:** Advanced  
> **Prerequisites:** LC-021, LC-024  
> **Book map:** Stallings Â Network Security Essentials Ch.4-5, 9; Ward Â How Linux Works Ch.5, 8
<!-- LC-CURRICULUM-END -->
## 📖 Daftar Isi / Table of Contents / 目次

| # | Topic | Bahasa Indonesia | English | 日本語 |
|---|-------|-----------------|---------|--------|
| 1 | Security Goals | Threat model & exposure | Threat model & exposure | セキュリティ目標 |
| 2 | Access Control Models | DAC, MAC, RBAC | DAC, MAC, RBAC | アクセス制御 |
| 3 | Defense-in-Depth | Layered controls | Layered controls | 多層防御 |
| 4 | SSH Hardening | OpenSSH baseline | OpenSSH baseline | SSH強化 |
| 5 | Fail2Ban | Auto-ban abuse | Intrusion prevention | Fail2Ban |
| 6 | Kernel `sysctl` | Network & memory | Kernel parameters | sysctl |
| 7 | AppArmor & SELinux | MAC confinement | MAC frameworks | MAC |
| 8 | Patch & Minimize | Updates & packages | Patch & minimize | 更新と最小化 |
| 9 | Security Notes | Defense checklist | Defense checklist | セキュリティノート |
| 10 | Cheatsheet | Referensi cepat | Quick reference | チートシート |

> **Hands-on firewall rules** (iptables, nftables, UFW, firewalld) → **note 27 · Firewall Setup**

---

## 1. 🎯 Linux Security Goals — Shrink the Attack Surface

### 🇮🇩 Bahasa Indonesia
Hardening = menurunkan eksposur tanpa merusak fungsi bisnis:

$$
\text{Risk} \propto \text{Assets} \times \text{Threats} \times \text{Vulnerabilities} \times \text{Exposure}
$$

Kita mengurangi **Exposure** (port, paket, user, privilege) dan **Vulnerabilities** (patch, config aman).

### 🇬🇧 English
A hardened host: few listeners, few packages, least privilege, enforced MAC, encrypted admin channels, and monitored auth logs.

### 日本語
公開面・権限・未修正脆弱性を計画的に減らします。

```
          Before                         After (sketch)
   ┌─────────────────┐              ┌─────────────────┐
   │ Many packages   │              │ Minimal packages│
   │ Open ports **** │     ───►     │ Only 22/443     │
   │ Password SSH    │              │ Keys only SSH   │
   │ MAC complain    │              │ MAC enforcing   │
   └─────────────────┘              └─────────────────┘
```

| Pillar | Example controls |
|:---|:---|
| Identity | Strong auth, no shared root SSH |
| Network | Default-deny firewall |
| Software | Signed updates, minimal set |
| Privilege | sudo scoped; no NOPASSWD:ALL |
| Detection | journal/auth → alerts |
| Recovery | Tested backups (note 21) |

---

## 2. 🗝️ Access Control — DAC, MAC, RBAC

| Model | Who decides? | Linux mechanism |
|:---|:---|:---|
| **DAC** | Resource owner | `chmod`/`chown` mode bits |
| **MAC** | System policy | SELinux, AppArmor |
| **RBAC** | Role assignment | groups + `sudo` + policy |

Unix mode bits (reminder):

$$
\text{mode} = \underbrace{s}_{\text{special}} \underbrace{rwx}_{\text{user}} \underbrace{rwx}_{\text{group}} \underbrace{rwx}_{\text{other}}
\quad\text{octal } 0\ldots777\ (+\, special\, 4/2/1\,000)
$$

Even correct DAC can be bypassed by a compromised `root` unless **MAC** confines processes.

---

## 3. 🏰 Defense-in-Depth — Layered Controls

### 🇮🇩 Bahasa Indonesia
Keamanan Linux efektif menumpuk kontrol independen — kegagalan satu lapisan tidak membebaskan host.

$$
\text{Effective security} = f(\text{Identity}, \text{Network}, \text{Host}, \text{App}, \text{Data}, \text{Detection})
$$

### 🇬🇧 English
| Layer | Linux examples | Deep-dive note |
|:---|:---|:---|
| Network edge | Router ACL, VPN | `networking/` module |
| **Host firewall** | iptables, UFW, firewalld | **note 27** |
| Transport/admin | SSH keys, TLS | This note §4 |
| OS identity | PAM, sudo, shadow | **note 15** |
| Object permissions | chmod, SUID audit | **note 14** |
| MAC | SELinux, AppArmor | §7 below |
| Monitoring | auth logs, journal | **note 28** |

### 日本語
ネットワーク→ホストFW→認証→MAC→ログの多層防御を設計します。

```
Internet
   │
   ▼
Edge firewall / WAF
   │
   ▼
Host firewall (note 27) ── default deny inbound
   │
   ▼
SSH/TLS hardened admin path (§4)
   │
   ▼
DAC + sudo RBAC + MAC (§2, §7)
   │
   ▼
Logging & alerting (note 28)
```

**Principle:** default-deny at network boundaries; least privilege everywhere; assume breach and limit blast radius.

---

## 4. 🔐 OpenSSH Hardening — `/etc/ssh/sshd_config`

### Baseline directives

| Directive | Hardened value | Purpose |
|:---|:---|:---|
| `PermitRootLogin` | `no` | No direct root sessions |
| `PasswordAuthentication` | `no` | Keys only |
| `PubkeyAuthentication` | `yes` | Enable keys |
| `KbdInteractiveAuthentication` | `no` | Reduce auth surfaces |
| `MaxAuthTries` | `3` | Slow guessing |
| `AllowUsers` / `AllowGroups` | explicit | Account allowlist |
| `X11Forwarding` | `no` (if unused) | Less attack surface |
| `ClientAliveInterval` | e.g. `300` | Drop stale sessions |

```bash
# Audit effective non-comment config
sudo sshd -T | egrep 'permitrootlogin|passwordauthentication|pubkeyauthentication|maxauthtries'

# Validate before restart
sudo sshd -t && sudo systemctl reload ssh
```

**Rollout tip:** keep a second session open while testing; use console/cloud serial if remote.

Generate admin keys (on admin workstation):

```bash
ssh-keygen -t ed25519 -a 100 -f ~/.ssh/id_ed25519_admin
# install public key to ~/.ssh/authorized_keys on server (mode 600)
```

---

## 5. 🚨 Fail2Ban — Automated Response

### 🇮🇩 Bahasa Indonesia
Fail2Ban mengamati log; setelah $N$ kegagalan dalam jendela waktu, IP di-ban di firewall selama $T$ detik.

$$
\text{ban}(ip) \iff \#\{\text{failures in window } W\} \ge N
$$

### 🇬🇧 English
Complements (does not replace) key-only SSH and firewall allowlists.

### 日本語
総当たりの自動遮断。鍵認証と併用します。

```bash
sudo apt install fail2ban -y
sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local
sudo systemctl enable --now fail2ban
sudo fail2ban-client status sshd
sudo fail2ban-client set sshd unbanip 203.0.113.50
```

Tune `findtime`, `maxretry`, `bantime` in jail.local; whitelist admin NATs carefully.

---

## 6. ⚙️ Kernel Hardening via `sysctl`

Useful defensive parameters (verify against your workload):

| Parameter | Value | Intent |
|:---|:---:|:---|
| `net.ipv4.tcp_syncookies` | `1` | SYN flood resilience |
| `net.ipv4.conf.all.accept_redirects` | `0` | Resist evil ICMP redirects |
| `net.ipv4.conf.all.accept_source_route` | `0` | Drop source-routed packets |
| `net.ipv4.conf.all.log_martians` | `1` | Log impossible addresses |
| `net.ipv4.icmp_echo_ignore_broadcasts` | `1` | Smurf-style noise |
| `kernel.randomize_va_space` | `2` | Full ASLR |
| `fs.protected_hardlinks` | `1` | Hardlink safety |
| `fs.protected_symlinks` | `1` | Symlink safety |
| `kernel.yama.ptrace_scope` | `1` or `2` | Limit process tracing |

```bash
sudo tee /etc/sysctl.d/99-hardening.conf << 'EOF'
net.ipv4.tcp_syncookies = 1
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.icmp_echo_ignore_broadcasts = 1
kernel.randomize_va_space = 2
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
EOF
sudo sysctl --system
cat /proc/sys/kernel/randomize_va_space   # expect 2
```

ASLR intuition: randomize base addresses so naive fixed-offset memory corruption is unreliable:

$$
\mathrm{P}(\text{guess correct slide}) \ll 1 \text{ under } \texttt{randomize\_va\_space}=2
$$

---

## 7. 🛡️ Mandatory Access Control — AppArmor & SELinux

| Framework | Distros | Model |
|:---|:---|:---|
| **AppArmor** | Ubuntu/Debian | Path-based profiles |
| **SELinux** | Fedora/RHEL/Alma/Rocky | Label/type enforcement |

```bash
# AppArmor
sudo aa-status
sudo aa-enforce /etc/apparmor.d/usr.sbin.nginx   # example

# SELinux
getenforce
sudo setenforce 1                                # enforcing until reboot policy
# persistent: SELINUX=enforcing in /etc/selinux/config
```

**Policy:** prefer **enforcing** in production; use complain/permissive only while developing profiles, then tighten.

---

## 8. 📦 Patch Quickly, Install Little

$$
\text{Attack surface} \uparrow \text{ with } |P_{\text{installed}}| \text{ and open listeners}
$$

```bash
# Debian/Ubuntu
sudo apt update && sudo apt upgrade
sudo apt install unattended-upgrades
sudo dpkg-query -f '${binary:Package}\n' -W | wc -l

# RHEL family
sudo dnf upgrade
sudo dnf install dnf-automatic

# Listeners audit
ss -tulnp
systemctl list-units --type=service --state=running
```

Disable unused services (`systemctl disable --now …`). Remove compilers and extra interpreters from production edge hosts if policy allows.

---

## 9. 🔐 Security Notes — DEFENSE Checklist

| Control area | Verify | Remediate |
|:---|:---|:---|
| Updates | Supported release; recent security upgrades | Enable auto security updates |
| SSH | `sshd -T` shows no password/root login | Keys + AllowUsers + firewall |
| Firewall | Default deny; minimal allows | UFW/nft rules reviewed |
| MAC | Enforcing profiles/labels | Fix denials properly — don’t disable MAC |
| Sudo | No broad `NOPASSWD: ALL` | Scope commands; use groups |
| SUID/SGID inventory | Periodic review of special bits | Remove unnecessary setuid |
| World-writable dirs | Especially cron/backup paths | Fix ownership & modes |
| Logs | auth + journal retained / forwarded | Note 28 practices |
| Backups | Encrypted + restore-tested | Note 21 practices |
| Firewall | Default deny; documented allows | Note 27 rules |
| Secrets | No keys in web roots / world-readable homes | `600` keys; vault/HSM |

```bash
# Defensive audit snippets (authorized admin)
sshd -T | egrep 'permitrootlogin|passwordauthentication'
sudo ufw status verbose
getenforce 2>/dev/null; sudo aa-status 2>/dev/null | head
find /usr -perm -4000 -type f 2>/dev/null | head   # inventory only
sudo grep -R "NOPASSWD" /etc/sudoers /etc/sudoers.d 2>/dev/null
```

> Inventory of SUID binaries is for **reduction & monitoring** on systems you administer — not a recipe list for abuse.

### Defense-in-depth stack
```
Users/Apps
   │  DAC (chmod) + sudo RBAC
   ▼
MAC (AppArmor/SELinux)
   │
   ▼
Kernel hardening (ASLR, sysctl)
   │
   ▼
Host firewall (note 27) + Fail2Ban
   │
   ▼
Network edge / WAF / VPN
```

---

## 10. 🧠 Quick Reference Cheatsheet

```bash
# Firewall → see note 27 for full iptables/UFW/firewalld workflows

# SSH
sudo sshd -t && sudo systemctl reload ssh
sudo sshd -T | egrep 'permitroot|passwordauthentication'

# Fail2Ban
sudo fail2ban-client status sshd

# sysctl
sudo sysctl --system
cat /proc/sys/kernel/randomize_va_space

# MAC
sudo aa-status
getenforce

# Surface
ss -tulnp
systemctl list-unit-files --state=enabled
```

---

> 📚 **References & Book Sources**
> - Brian Ward — *How Linux Works: What Every Superuser Should Know* — `~/Documents/Books/CyberSec/Linux/`
> - William Shotts — *The Linux Command Line* — `~/Documents/Books/CyberSec/Linux/`
> - William Stallings — *Network Security Essentials* (access control & hardening concepts)
> - CIS Linux Benchmarks (distribution-specific) — configuration baselines
> - `man ufw`, `man sshd_config`, `man sysctl`, `man fail2ban-client`, `man apparmor`, `man sestatus`

> 🔖 **Repository:** [LearnCybersecurity](https://github.com/KodoktheGr3at/LearnCybersecurity)  
> ⚖️ Edukasi hardening & defense-in-depth — bukan panduan bypass, privesc, atau GTFOBins abuse.
