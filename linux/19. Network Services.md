# 🌐 Network Services — DNS, DHCP, NFS & File Sharing

> **LearnCybersecurity** | Linux Fundamentals Series  
> 📅 Last Updated: 2026 | 👤 Author: kodoktheGr3at  
> 📚 Primary refs: Brian Ward — *How Linux Works* (`~/Documents/Books/CyberSec/Linux/`); Christian Benvenuti — *Understanding Linux Network Internals* (`~/Documents/Books/CyberSec/Networking/`)

---


<!-- LC-CURRICULUM-START -->
> **Curriculum ID:** `LC-026` | **Phase 1:** Linux  
> **Est. study:** 4h | **Level:** Intermediate  
> **Prerequisites:** LC-024, LC-038  
> **Book map:** Benvenuti Â Understanding Linux Network Internals Ch.9-12; Stevens Â UNIX Network Programming Vol.1 Ch.9-11
<!-- LC-CURRICULUM-END -->
## 📖 Daftar Isi / Table of Contents / 目次

| # | Topic | Bahasa Indonesia | English | 日本語 |
|---|-------|-----------------|---------|--------|
| 1 | Network Service Model | Peran daemon jaringan | Role of network daemons | ネットワークデーモンの役割 |
| 2 | Name Resolution | DNS & `/etc/hosts` | DNS & local name tables | 名前解決 |
| 3 | DHCP Client | Alokasi IP otomatis | Automatic IP assignment | DHCPクライアント |
| 4 | NFS | Network File System | NFS exports & mounts | NFS |
| 5 | Samba / SMB | Berbagi file Windows–Linux | SMB/CIFS file sharing | Samba |
| 6 | Service Discovery | Port & socket audit | Listening service audit | 待受サービスの監査 |
| 7 | Security Notes | Risiko & hardening | Risks & hardening | セキュリティノート |
| 8 | Cheatsheet | Referensi cepat | Quick reference | チートシート |

---

## 1. 🌐 Network Services on Linux — The Daemon Layer

### 🇮🇩 Bahasa Indonesia
**Network service** adalah daemon yang mendengarkan socket (TCP/UDP) dan melayani protokol jaringan: DNS, DHCP, NFS, SMB, SSH, HTTP, dan sejenisnya. Administrator Linux harus tahu **siapa yang listen**, **port berapa**, dan **konfigurasi di mana**.

$$
\text{Client request} \xrightarrow{\text{L3/L4}} \text{Listener (port)} \rightarrow \text{Daemon} \rightarrow \text{Config + data}
$$

### 🇬🇧 English
Linux hosts often run both **client** and **server** network roles. Client config lives in `/etc/resolv.conf`, NetworkManager, or systemd-networkd; server roles use dedicated packages (`bind9`, `isc-dhcp-server`, `nfs-kernel-server`, `samba`).

### 日本語
Linux 上のネットワークサービスはソケットを待ち受け、プロトコルを処理するデーモンです。誰がどのポートで listen し、設定がどこにあるかを把握することが運用の基本です。

```
Application                Kernel network stack
     │                              │
     │                              │
  dig/curl/mount.nfs  ←→  socket bind :53/:445/:2049
                                    │
                                    ▼
                            named / smbd / nfsd
```

| Service | Default port(s) | Package (Debian) | Config hub |
|:---|:---:|:---|:---|
| DNS resolver (client) | — | `systemd-resolved` | `/etc/resolv.conf`, `/etc/systemd/resolved.conf` |
| DNS server | 53/tcp,udp | `bind9` | `/etc/bind/` |
| DHCP server | 67/udp | `isc-dhcp-server` | `/etc/dhcp/dhcpd.conf` |
| NFS | 2049/tcp | `nfs-kernel-server` | `/etc/exports` |
| Samba (SMB) | 445/tcp | `samba` | `/etc/samba/smb.conf` |
| SSH | 22/tcp | `openssh-server` | `/etc/ssh/sshd_config` |

---

## 2. 🔎 Name Resolution — DNS & `/etc/hosts`

### 🇮🇩 Bahasa Indonesia
Sebelum query DNS eksternal, Linux memeriksa **`/etc/hosts`** (statis), lalu resolver (biasanya **`systemd-resolved`** atau **`/etc/resolv.conf`**).

Urutan resolusi (NSS — `/etc/nsswitch.conf`):

$$
\text{gethostbyname} \rightarrow \texttt{files} \rightarrow \texttt{dns} \rightarrow \texttt{\ldots}
$$

### 🇬🇧 English
Use `dig`, `host`, or `resolvectl` to debug resolution. Misconfigured DNS leaks queries or enables spoofing on untrusted networks — prefer trusted resolvers and DNSSEC where policy requires.

### 日本語
`/etc/hosts` を先に参照し、続いて DNS リゾルバへ問い合わせます。診断には `dig` / `resolvectl` を使い、信頼できるリゾルバと必要なら DNSSEC を選びます。

```bash
# Static local names (lab VMs you administer)
cat /etc/hosts
# 127.0.0.1   localhost
# 10.10.14.5  target.lab

# Resolver state
resolvectl status
cat /etc/resolv.conf

# Query DNS (authorized targets)
dig A example.com +short
dig MX example.com +short
host -t TXT example.com
nslookup example.com
```

| File / tool | Role |
|:---|:---|
| `/etc/hosts` | Static hostname → IP |
| `/etc/resolv.conf` | Nameserver list (often symlink to systemd-resolved) |
| `/etc/systemd/resolved.conf` | Stub listener `127.0.0.53`, DNS over TLS options |
| `dig` / `host` | Diagnostic queries |
| `systemd-resolved` | Caching stub resolver on modern distros |

**Minimal BIND server sketch (lab only):**

```bash
sudo apt install bind9 -y
# /etc/bind/named.conf.local — zone definition for lab.internal
sudo named-checkconf
sudo systemctl reload bind9
dig @127.0.0.1 lab.internal SOA
```

---

## 3. 📡 DHCP — Dynamic Host Configuration

### 🇮🇩 Bahasa Indonesia
**DHCP** memberikan IP, gateway, DNS, dan opsi lain ke klien. Di Linux desktop/server, klien biasanya **`NetworkManager`** atau **`dhclient`**; server DHCP jarang di edge kecuali lab.

### 🇬🇧 English
Client lease lifecycle: DISCOVER → OFFER → REQUEST → ACK. Renewals happen at $T/2$. Monitor leases on the server side for rogue DHCP (an attacker on the LAN offering a bad gateway/DNS).

### 日本語
DHCP は IP・ゲートウェイ・DNS を自動配布します。リースは DISCOVER → OFFER → REQUEST → ACK で確立し、$T/2$ で更新されます。LAN 上の不正 DHCP に注意します。

```bash
# Client: view lease (NetworkManager)
nmcli dev show eth0 | grep -E 'IP4.ADDRESS|IP4.GATEWAY|IP4.DNS'

# Client: release / renew (legacy)
sudo dhclient -r eth0
sudo dhclient eth0

# Server config snippet (/etc/dhcp/dhcpd.conf) — lab
# subnet 10.10.14.0 netmask 255.255.255.0 {
#   range 10.10.14.100 10.10.14.200;
#   option routers 10.10.14.1;
#   option domain-name-servers 10.10.14.1;
# }
sudo systemctl enable --now isc-dhcp-server
```

| DHCP risk | Mitigation |
|:---|:---|
| Rogue DHCP on LAN | DHCP snooping on switches; 802.1X |
| Stale leases | Short lease times in dynamic pools |
| No logging | Log assignments on authoritative server |

---

## 4. 📁 NFS — Network File System

### 🇮🇩 Bahasa Indonesia
**NFS** mem-mount direktori remote via kernel client. Server mendefinisikan **export** di `/etc/exports`; klien **`mount -t nfs`**.

### 🇬🇧 English
NFSv4 simplifies ports (2049/tcp) vs legacy NFSv3 (rpcbind + multiple ports). **Never** export world-writable paths without strong auth; prefer Kerberos (`sec=krb5p`) in production.

### 日本語
NFS はリモートディレクトリをカーネルクライアントでマウントします。本番では広いサブネットへの書き込み export を避け、Kerberos (`sec=krb5p`) を推奨します。

```bash
# Server
sudo apt install nfs-kernel-server -y
echo '/srv/nfs/share 10.10.14.0/24(rw,sync,no_subtree_check)' | sudo tee -a /etc/exports
sudo exportfs -ra
sudo systemctl enable --now nfs-server
showmount -e localhost

# Client (authorized lab network)
sudo mkdir -p /mnt/nfs_share
sudo mount -t nfs 10.10.14.10:/srv/nfs/share /mnt/nfs_share
df -hT /mnt/nfs_share
# Persistent: add to /etc/fstab
# 10.10.14.10:/srv/nfs/share  /mnt/nfs_share  nfs  defaults  0  0
```

| Export option | Meaning |
|:---|:---|
| `rw` / `ro` | Read-write vs read-only |
| `sync` | Safer; `async` faster but riskier on crash |
| `no_root_squash` | **Dangerous** — remote root maps to local root |
| `root_squash` | Default safer mapping |

---

## 5. 🪟 Samba — SMB/CIFS File Sharing

### 🇮🇩 Bahasa Indonesia
**Samba** (`smbd`/`nmbd`) melayani protokol Windows **SMB** sehingga Linux bisa berbagi folder ke Windows/macOS atau mount share Windows.

### 🇬🇧 English
Configure shares in `/etc/samba/smb.conf`. Use **`smbpasswd`** for Samba users (separate from Unix password unless integrated). Test with `testparm` before reload.

### 日本語
Samba は SMB/CIFS で Windows とファイル共有します。`smb.conf` を編集したら必ず `testparm` で構文確認してから再読込します。

```bash
sudo apt install samba -y
sudo testparm -s

# Minimal share block in smb.conf:
# [shared]
#   path = /srv/samba/shared
#   browseable = yes
#   read only = no
#   valid users = alice

sudo smbpasswd -a alice
sudo systemctl enable --now smbd nmbd

# Client mount (authorized)
sudo mount -t cifs //10.10.14.10/shared /mnt/smb -o username=alice,uid=1000
smbclient -L //10.10.14.10 -U alice
```

| Check | Command |
|:---|:---|
| Config syntax | `testparm` |
| Active shares | `smbstatus` |
| Listening | `ss -tlnp \| grep smbd` |

---

## 6. 🔍 Auditing Listening Network Services

### 🇮🇩 Bahasa Indonesia
Inventarisasi rutin: port terbuka, proses pemilik, unit systemd terkait. Setiap listener harus punya alasan bisnis.

### 🇬🇧 English
Map every listener to a business need. Unexpected `0.0.0.0` binds are high-priority findings.

### 日本語
待受ポート・所有者プロセス・systemd ユニットを定期監査します。`0.0.0.0` への予期しない bind は優先度の高い発見です。

```bash
# Modern socket audit
ss -tulnp
ss -tulnp | grep LISTEN

# Legacy (may be absent on minimal images)
netstat -tulnp 2>/dev/null

# Map to systemd units
systemctl list-units --type=service --state=running | grep -E 'ssh|nfs|samba|bind|apache|nginx'

# What owns port 445?
sudo lsof -i :445
sudo fuser -v 445/tcp
```

$$
\text{Exposure score} \uparrow \text{ with listeners on } 0.0.0.0 \text{ and weak auth services}
$$

---

## 7. 🔐 Security Notes — DEFENSE / Hardening

### 🇮🇩 Bahasa Indonesia
Layanan jaringan memperbesar permukaan serangan. Default: **jangan expose** ke internet kecuali perlu; sempitkan export/share; audit listener.

### 🇬🇧 English
Treat every extra daemon as extra exposure. Recursion, guest shares, and `no_root_squash` are classic high-impact misconfigurations.

### 日本語
不要なデーモンを公開せず、再帰 DNS・ゲスト共有・`no_root_squash` などの定番誤設定を重点的に監査します。

| Misconfiguration | Impact | Defense |
|:---|:---|:---|
| NFS `no_root_squash` + wide subnet | Remote root file write | `root_squash`; narrow exports; Kerberos |
| Samba guest write | Anonymous malware drop | Disable guest; auth required |
| Open DNS recursion to internet | Amplification abuse | Recursion allowlist only |
| Rogue DHCP | MITM gateway/DNS | DHCP snooping; static on servers |
| Stale `/etc/hosts` overrides | Wrong routing to internal apps | Configuration management |
| Unencrypted NFSv3 on untrusted LAN | Data sniffing | NFSv4 + Kerberos or VPN |

```bash
# Defensive audit (authorized admin)
grep -v '^#' /etc/exports 2>/dev/null
testparm -s 2>/dev/null | head -30
ss -tulnp | awk 'NR==1 || /0.0.0.0|:::/'
resolvectl dns
```

Cross-reference: host firewall rules → **note 27**; interface config → **notes 24–25**.

---

## 8. 🧠 Quick Reference Cheatsheet

```bash
# DNS
dig A example.com +short
resolvectl status
cat /etc/hosts

# DHCP client
nmcli dev show eth0

# NFS
showmount -e SERVER
sudo mount -t nfs SERVER:/export /mnt/nfs

# Samba
testparm -s
smbclient -L //SERVER -U user

# Audit
ss -tulnp
systemctl list-units --type=service --state=running
```

---

> 📚 **References & Book Sources**
> - Brian Ward — *How Linux Works: What Every Superuser Should Know* — `~/Documents/Books/CyberSec/Linux/`
> - Christian Benvenuti — *Understanding Linux Network Internals* — `~/Documents/Books/CyberSec/Networking/`
> - W. Richard Stevens — *UNIX Network Programming* — `~/Documents/Books/CyberSec/Networking/`
> - `man exports`, `man smb.conf`, `man systemd-resolved`, `man dig`, `man ss`

> 🔖 **Repository:** [LearnCybersecurity](https://github.com/Kodokthegr3at/LearnCybersecurity)  
> ⚖️ Edukasi administrasi layanan jaringan berizin — bukan panduan rogue DHCP/DNS abuse.
